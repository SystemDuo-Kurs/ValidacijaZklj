<Window x:Class="ValidacijaZklj.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ValidacijaZklj"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" FontSize="24">
	<StackPanel>
		<TextBox Text="{Binding Path=Broj, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource StilZaTextBox}"/>
		<TextBox Text="{Binding Path=Tekst, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource StilZaTextBox}"/>
		<Button Click="Asd">Teest</Button>
	</StackPanel>
</Window>
